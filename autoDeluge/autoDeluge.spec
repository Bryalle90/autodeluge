# -*- mode: python -*-
a = Analysis(['autoDeluge.py'],
             pathex=['G:\\Projects\\autoDeluge\\autoDeluge'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='autoDeluge.exe',
          debug=False,
          strip=None,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='autoDeluge')
